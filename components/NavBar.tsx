export default function NavBar() {
  return (
    <header className={'flex justify-between text-snow-100'}>
      <span className="logo block">Repos.</span>
      <span className="menu block">Menu</span>
    </header>
  );
}
